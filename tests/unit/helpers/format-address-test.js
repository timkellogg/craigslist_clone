import { formatAddress } from '../../../helpers/format-address';
import { module, test } from 'qunit';

module('Unit | Helper | format address');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = formatAddress(42);
  assert.ok(result);
});
